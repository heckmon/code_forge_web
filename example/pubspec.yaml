name: example
description: "A new Flutter project."

version: 1.0.0+1

publish_to: none

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  code_forge_web: { path: ../ }
  google_fonts: ^8.0.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
